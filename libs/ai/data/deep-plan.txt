You are planning a data investigation for a crypto DCA trading bot. The user asked one question that is best answered by gathering several pieces of data first.

Break the question into 2 to 4 focused, independent sub-questions that together fully answer it. Each sub-question must be answerable from the bot's own read-only data (deals, orders, bots, pairs, balances, exposure, logs, performance over time). Make each one concrete and specific — name the pair, bot, deal, or time window when the question implies one. Do not include steps that require changing anything; everything is read-only.

Prefer fewer, higher-value sub-questions over many shallow ones. Do not restate the original question verbatim as a sub-question.

Return ONLY JSON in this exact shape, nothing else:
{"subquestions": ["...", "..."]}
